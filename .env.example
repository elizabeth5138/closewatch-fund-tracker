# Raw daily close and volume provider. Never commit a real key.
ALPHA_VANTAGE_API_KEY=

# Long random bearer token for POST /api/ingest. Never expose it in browser code.
INGEST_TOKEN=

# Explicitly show labelled sample rows. Remove or set false for live mode.
PREVIEW_MODE=false
